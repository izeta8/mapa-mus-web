
export interface MatchupCardStyles {
    container: string;
    numero: string;
    mesa: string;
    vs: string;
    badge: string;
    cardWidth: number;
    cardHeight: number;
    baseFontSize: number;
}